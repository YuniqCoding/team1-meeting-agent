# 우리 팀 담당자 명단

> 부르는 이름은 **보여주기용**입니다. 정의 파일(`.claude/agents/`)의 "언제 부르나" 칸에는 넣지 않습니다.
> 거기에 별명을 넣으면 불려야 할 때 안 불려 나옵니다.

| 칸 이름 | 부르는 이름 | 하는 일 한 줄 | 맡은 사람 |
| --- | --- | --- | --- |
| collect | 모으미 | 회의록 원문에서 개요와 발언만 골라 모은다 | 최유라 |
| summarize | 간추리 | 결정된 것만 골라 핵심 세 줄과 할 일로 줄인다 | 최유라 |
| verify | 꼼꼼이 | 항목마다 통과와 반려를 가른다 | 최유라 |
| report | 마무리 | 통과한 것만 모아 보고서를 만든다 | 최유라 |

## 파이프라인 순서

```
collect(모으미) → summarize(간추리) → verify(꼼꼼이) → report(마무리)
```
